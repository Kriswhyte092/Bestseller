.left-sidebar {
  background-image: -webkit-radial-gradient(circle, #0d2e54, #1a1a2e 60%); /* Chrome, Safari */
  background-image: -moz-radial-gradient(circle, #0d2e54, #1a1a2e 60%);    /* Firefox */
  background-image: -ms-radial-gradient(circle, #0d2e54, #1a1a2e 60%);     /* Internet Explorer 10+ */
  background-image: -o-radial-gradient(circle, #0d2e54, #1a1a2e 60%);      /* Opera */
  background-image: radial-gradient(circle, #0f3460, #1a1a2e 60%);         /* Standard */
  background-color: #1a1a2e; /* Fallback for very old browsers */
  width: 250px;
  padding: 20px;
  height: 100vh; /* Ensure it takes the full viewport height */
  position: fixed; /* Fix the sidebar position */
  top: 0;
  left: 0;
  overflow-y: auto; /* Add vertical scroll if content overflows */
  color: white; /* Ensure text is readable */
}
.right-sidebar {
  width: 300px;
  background: linear-gradient(135deg, #f5f5f5, #e0e0e0);
  padding: 20px;
  box-shadow: -2px 0 10px rgba(0, 0, 0, 0.1); /* Softer shadow for a sleeker look */
  height: 100%;
  border-left: 1px solid rgba(0, 0, 0, 0.05); /* Subtle border for separation */
  border-radius: 0 8px 8px 0; /* Rounded corners on the left side */
}
.container .main {
    background-color: red;
  }
  /*þetta fer allt i stules.css